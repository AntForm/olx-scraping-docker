Requirements: you should have installed:
* git
* docker
* docker-compose

Installation:
* git clone https://github.com/AntForm/olx-scraping-docker.git && cd ./olx-scraping-docker && docker-compose up --build'
* go to http://localhost:8000
* PROFIT
